# AGENTS.md

このリポジトリで作業するエージェント向けの追加ルールです。上位のシステム/開発者指示を優先し、その上でこのファイルを守ってください。

## プロジェクト方針

- このゲームは、競技・ミッション達成ではなく、アリの行列が自律的に動き、プレイヤーの干渉で変化する様子を観察するブラウザゲームです。
- スコア、クリア条件、強い誘導文、お題形式の UI は追加しないでください。
- プレイヤー操作は「倒す」「罰する」よりも、フェロモン場・通行可能性・個体の向きに外乱を与えるものとして設計してください。
- アリを気持ち悪く描く方向ではなく、くすみ系で観察しやすい表現を維持してください。

## アリ行動モデルの根拠

行動モデルを変更する前に、必ず `docs/ant-behavior-model.md` を読んでください。

実装上の基本方針:

- フェロモン追従は、左右の濃度差を総量で割る Weber 型に近い応答を基準にする。
- 個体は完全な経路計画を持たず、ランダム探索、フェロモン追従、弱い経路記憶、障害物回避の合成で動かす。
- フェロモンは蒸発・拡散し、行列は固定線ではなく時間で薄くなったり再形成されたりするものとして扱う。
- 過剰フェロモン、濡れた場所、指跡などは「本物の感情」ではなく、trail integrity を乱す外乱として扱う。
- 種に依存する行動を一般的な「アリの本能」と断定しないでください。

## 事実確認ルール

- アリの生態や研究に関する表示文・README・docs を変えるときは、一次資料、査読論文、大学/政府/出版社ページを優先して確認してください。
- 実装都合の表現は「ゲーム表現」「簡略モデル」と明記してください。
- 研究由来の主張を追加した場合は、`docs/ant-behavior-model.md` の参照資料か評価表も更新してください。

## 実装時の注意

- `AntColonySimulation` の変更では、10倍速時にも NaN/Infinity、アリ数の暴走、オブジェクトの残留が起きないよう確認してください。
- 10倍速は一時効果ではなく維持トグルです。マップ切替・リセット後もユーザーが選んだ速度を保つ設計を壊さないでください。
- 初期状態は完全ランダムではなく、10パターンから選ぶ設計です。パターンを変える場合も、餌2か所は「まれ」に留めてください。
- スコアを復活させないでください。観測値としては「運んだカケラ」「アリ数」「大物」「行列安定度」程度に抑えてください。

## 検証

変更後は最低限以下を確認してください。

- `npm run build`
- ブラウザで通常速度と10倍速を切り替え
- 10倍速のままマップ切替とリセット
- 数十秒以上の起動で直近のブラウザエラーがないこと
- スコア/お題/連鎖の UI が復活していないこと
